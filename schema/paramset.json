{
    "$schema": "http://json-schema.org/schema#",
    "$id": "paramset.json#",
    "type": "object",
    "properties": {
        "name": {
            "type": "string"
        },
        "comment": {
            "type": "string"
        },
        "security level": {
            "type": "object",
            "properties": {
                "classical": {
                    "type": "integer",
                    "minimum": 1,
                    "$comment": "measured in bits"
                },
                "quantum": {
                    "type": "integer",
                    "minimum": 1,
                    "$comment": "measured in bits"
                },
                "nist category": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 5
                },
                "comment": {
                    "type": "string"
                }
            },
            "required": [
                "classical",
                "quantum"
            ],
            "$comment": "#XXX Do we really always have a quantum estimation?"
        },
        "failure probability": {
            "type": "number",
            "maximum": 0,
            "$comment": "0 for 0, log_2 else"
        },
        "failure probability comment": {
            "type": "integer"
        },
        "sizes": {
            "type": "object",
            "$comment": "Measured in bytes.",
            "properties": {
                "sk": {
                    "type": "integer",
                    "minimum": 1
                },
                "pk": {
                    "type": "integer",
                    "minimum": 1
                },
                "msg": {
                    "type": "integer",
                    "minimum": 1,
                    "$comment": "For KEMs, size of the shared secret. #TODO signatures?"
                },
                "ct/sig": {
                    "type": "integer",
                    "minimum": 1
                },
                "comment": {
                    "type": "integer"
                }
            },
            "additionalProperties": false,
            "required": ["sk", "pk", "msg", "ct/sig"]
        }
    },
    "additionalProperties": false,
    "required": [
        "name",
        "security level",
        "failure probability",
        "sizes"
    ]
}
